{
  "name": "parcel-plugin-multiloading-assets-bug-demo",
  "version": "1.0.0",
  "description": "Parcel plugin (MyAsset) shows how assets are being loaded more than once on Parcel startup",
  "license": "ISC",
  "scripts": {
    "testServe": "node testServe.js",
    "testServeLimitParcelWorkers": "PARCEL_WORKERS=1 node testServe",
    "testBundle": "node testBundle.js"
  },
  "peerDependencies": {
    "parcel-bundler": "^1.10.3"
  },
  "devDependencies": {
    "parcel-bundler": "^1.10.3"
  }
}
